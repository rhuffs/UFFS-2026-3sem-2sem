Arvores binarias, 

lista dupla encadeada é uma estrutura que aponta para o proximo, mas tambem para o interior.

first(head), last(tail).

ld (*head, *tail) Sentinelas


typedef struct tsent{ 
    ld *head, *tail;
}sent;

