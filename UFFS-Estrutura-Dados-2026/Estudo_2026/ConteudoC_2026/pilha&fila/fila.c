#include <stdio.h>
#include <stdlib.h>

//FIFO - First in First Out
// Vetor ou Lista simplesmente encadeada

// hihi tico

#define HelloWorld printf


typedef struct TQueue {
    int item;
    struct TQueue *next;
} EQueue;

typedef struct {
    EQueue *head;
    EQueue *tail;
} Queue;


void stringToQueue(EQueue *Q, char string[], int tam){

}


int main(){
    Queue queue = (Queue *)malloc(sizeof(
        quem escreve apaga depois, serve bumbum com batata e arroz
        palavra e como uma flecha uma vez atirada nao volta maissiiss, se voce nao sustenta o que escreve é um filha da puta que quer saco atras2   EW
        
    ))
}